# Example Module - Terraform

> **Note:** This is sample code intended to be seeded into the repositories created by this module. It is not a standalone Terraform module and should not be used directly. It serves as a starting point that users can customize for their own infrastructure deployments.

This example Terraform configuration deploys a simple set of Azure resources (resource group, virtual network, and virtual machine) to demonstrate the CI/CD workflow structure.
